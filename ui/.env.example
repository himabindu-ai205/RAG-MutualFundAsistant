# Local Vite (optional). Leave empty to use the /chat proxy in vite.config.ts.
# VITE_API_BASE_URL=

# Production (Vercel): set in Project → Settings → Environment Variables
# VITE_API_BASE_URL=https://your-api.up.railway.app
